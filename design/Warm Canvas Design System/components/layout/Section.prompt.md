Band wrapper: 96px vertical rhythm, 1200px centred container.

```jsx
<Section tone="cream">…feature grid…</Section>
```

Tones: `canvas`, `soft`, `cream`, `creamStrong`, `dark`. Never repeat a tone in two consecutive bands.
