Cream hero — 6/6 grid, 64px serif h1 left, artifact right, 96px padding.

```jsx
<HeroBand title="Meet your thinking partner" subtitle="…" actions={<Button>Try Claude</Button>}
  artifact={<HeroIllustrationCard tone="dark">{mockup}</HeroIllustrationCard>} />
```

Omit `artifact` for a single-column editorial hero.
