16px-radius container for the hero's right-hand artifact.

```jsx
<HeroIllustrationCard tone="dark" caption="Agent run, live">{codeWindow}</HeroIllustrationCard>
```

`cream` for line-art illustrations, `dark` for product chrome. The only place 16px radius is used.
