Filter row used on solutions and connectors pages.

```jsx
<CategoryTabs tabs={["All", "Productivity", "Developer tools"]} value={tab} onChange={setTab} />
```

Active tab = `--surface-card` fill + ink text. Nothing else changes; there is no underline or coral indicator in this system.
