Pill label for categories and status flags.

```jsx
<Badge tone="cream">Engineering</Badge>
<Badge tone="coral">New</Badge>
```

`cream` renders 13px sentence case; `coral`, `amber` and `teal` render 12px uppercase with 1.5px tracking — the flag treatment. Use `onDark` inside navy cards.
