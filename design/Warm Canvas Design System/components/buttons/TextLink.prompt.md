Inline coral link for running text and "learn more" affordances.

```jsx
<p className="ds-body-md">Compare the models in the <TextLink href="/docs">model guide</TextLink>.</p>
```

Tones: `coral` (default, body), `ink`, `onDark`, `muted` (footer columns). Underlines on press only — never a hover treatment beyond the system default.
