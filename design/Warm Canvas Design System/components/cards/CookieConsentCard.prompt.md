Floating navy consent card, pinned bottom-right at 24px inset.

```jsx
<CookieConsentCard onAccept={hide} onReject={hide}>We use cookies to understand how the site is used.</CookieConsentCard>
```

Max width 380px. Buttons are `secondaryOnDark` + `textOnDark`.
