Navy card that frames real product chrome inside a lighter inner well.

```jsx
<ProductMockupCard label="Agent runs" caption="Claude working across a repo">{chrome}</ProductMockupCard>
```

Show actual UI fragments — chat rows, agent steps, file trees. Never a marketing illustration of the product.
