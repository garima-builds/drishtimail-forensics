Cream card for 3-up feature grids — 12px radius, 32px padding, no shadow, no border.

```jsx
<FeatureCard title="Connect your tools" eyebrow="Context">Pull answers from the systems your team already works in.</FeatureCard>
```

Set `tone="dark"` only when the grid sits inside a navy band. `footer` takes a TextLink.
