Full-bleed coral card — the one generous use of the brand colour.

```jsx
<CalloutCard title="Start building today" actions={<Button variant="onCoral">Get an API key</Button>}>Ship your first agent in an afternoon.</CalloutCard>
```

One per page at most. Buttons inside must be `onCoral`.
