Cream + hairline card for one model tier. Name is display serif; spec values are mono.

```jsx
<ModelComparisonCard name="Opus" tagline="For the hardest problems"
  meta={[{label:"Context",value:"200K"}]} link={<TextLink>Read the model guide</TextLink>} />
```

Use in a 3-up row (Opus / Sonnet / Haiku).
