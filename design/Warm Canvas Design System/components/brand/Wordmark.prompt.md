Type-set brand lockup for nav bars and footers.

```jsx
<Wordmark name="Claude" />
<Wordmark name="Anthropic" tone="onDark" size={18} />
```

No glyph is drawn — the design system was supplied without logo files. Drop a real SVG into `assets/` and extend this component when one is available.
